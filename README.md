# my-docker-bp
Docker Boilerplate

```
1. Install Docker https://docs.docker.com/docker-for-mac/install/

In terminal
1. cd my-docker-bp
2. docker-compose build
3. docker-compose up (-d to exit out back to prompt)
To terminate
1. dock-compose down
2. (do docker system prune once and a while)
```
